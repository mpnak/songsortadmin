<div class="new-station">
  (12 Station max for now)
  <%= link_to 'NEW STATION', new_station_path, class: 'btn btn-default' %>
</div>

<div class="stations">
  <% @stations.each do |station| %>

    <%= link_to station, class: "station" do %>

      <% if station.station_art %>
        <%= image_tag station.station_art, class: "station-art" %>
      <% else %>
        <div class="station-content">
          <%= station.name %>
        </div>
      <% end %>

      <% if station.active %>
        <div class="active-status active">LIVE</div>
      <% else %>
        <div class="active-status inactive">INACTIVE</div>
      <% end %>

    <% end %>
  <% end %>
</div>
